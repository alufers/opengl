#ifndef MATERIAL_H
#define MATERIAL_H
#include "ShaderProgram.h"
#include <map>
#include <memory>
#include <string>
/*
class Material {
public:
  std::shared_ptr<ShaderProgram> shaderProgram;
  std::map<std::string, uniformValue> uniforms;
  Material(ShaderProgram *shaderProgram, std::map<std::string, uniformValue> uniforms);
  void use();
};*/
#endif
