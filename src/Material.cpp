#include "Material.h"
/*
Material::Material(ShaderProgram *shaderProgram, std::map<std::string, uniformValue> uniforms) {
    this->shaderProgram = std::make_shared<ShaderProgram>(shaderProgram);
}

void Material::use() {
    for(auto const u : this->uniforms) {
        this->shaderProgram->setUniform(u.first, u.second);
    }
    this->shaderProgram->use();
}
*/
